<?xml version="1.0" encoding="utf-8"?>
<!--
  Widget de Streak — Modo ESCURO
  Fundo: preto/cinza escuro | Detalhes: roxo | Texto: branco
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_bg_dark"
    android:padding="16dp"
    android:gravity="center_vertical">

    <!-- Linha superior: emoji + título -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="8dp">

        <TextView
            android:id="@+id/widget_fire_emoji"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🔥"
            android:textSize="22sp"
            android:layout_marginEnd="8dp" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Sequência de Estudos"
            android:textColor="#CCCCCC"
            android:textSize="11sp"
            android:textAllCaps="true"
            android:letterSpacing="0.08" />

    </LinearLayout>

    <!-- Contador principal de streak -->
    <TextView
        android:id="@+id/widget_streak_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="0 dias"
        android:textColor="#FFFFFF"
        android:textSize="32sp"
        android:textStyle="bold"
        android:layout_marginBottom="2dp" />

    <!-- Recorde -->
    <TextView
        android:id="@+id/widget_best_streak"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Recorde: 0 dias"
        android:textColor="#9B6DFF"
        android:textSize="12sp"
        android:layout_marginBottom="8dp" />

    <!-- Divisor -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#333333"
        android:layout_marginBottom="8dp" />

    <!-- Questões e acerto -->
    <TextView
        android:id="@+id/widget_questions"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="0 questões · 0% acerto"
        android:textColor="#AAAAAA"
        android:textSize="11sp"
        android:layout_marginBottom="4dp" />

    <!-- Mensagem motivacional -->
    <TextView
        android:id="@+id/widget_motivation"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Comece hoje!"
        android:textColor="#7C4DFF"
        android:textSize="12sp"
        android:textStyle="italic" />

</LinearLayout>
