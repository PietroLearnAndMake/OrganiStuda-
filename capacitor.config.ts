import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pietro.organizador.enem',
  appName: 'Organizador Enem',
  webDir: 'dist'
};

export default config;
